import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  authHeading: {
    fontWeight: 'bold',
    fontSize: 30,
    marginBottom: 50,
    textAlign: 'center',
  },
});

export default globalStyles;
